```mermaid
sequenceDiagram
    participant browser
    participant server

    activate browser
    Note right of browser: user submits new note
    browser ->> browser: prevent default form submit
    browser ->> browser: add note to local notes array
    Note right of browser:SPA renders the updated notes list


    browser ->>server: POST /exampleapp/new_note_spa {content, date}
    deactivate browser
    activate server
    Note left of server: Server saves the new note
    server -->>browser: status code : 201 created
    deactivate server

    